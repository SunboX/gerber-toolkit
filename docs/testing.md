# Testing

Run the package tests with:

```bash
npm test
```

Tests use synthetic Gerber, Excellon, and ZIP payloads generated in code. Production fabrication packages and source-derived fixture names must not be committed.
